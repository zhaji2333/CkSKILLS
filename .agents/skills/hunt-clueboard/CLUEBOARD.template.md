# 线索板 · <目标短名>

> 工作记忆，不是日记。只写判断和出处，不贴整份 JS、不写完整 PII。
> 状态只用：`假设` / `已证伪` / `已证实` / `待打`
> 技能：读写纪律见 `hunt-clueboard`；指纹分类见 `unauth-path-key-hunt` P2；JS 拆包见 `recon-js-analysis`

- 目标：
- 开工日：
- 约束：零身份 / 不依赖登录 Network 当发现源
- 当前焦点：（一句话，下一轮先做这件事）

## 0. 当前假设（最多 5 条）

| 假设 | 状态 | 怎么证伪 | 结果 |
|---|---|---|---|
| | 假设 | | |

## 1. Host 地图

| Host | 类（壳/网关/业务H5/后台SSO） | IP | 指纹（码/类型/Server/body） | 下一步 |
|---|---|---|---|---|
| | | | | |

## 2. 路径 / 方法

| 方法 | 出处（JS/urlscan/兄弟域/命名） | 首次指纹 | 鉴权（无票/SSO/ACL） | 状态 |
|---|---|---|---|---|
| | | | | |

## 3. 密钥 / 协议

- 取钥接口：
- 算法与填充：
- 绑定字段名：（写 JS 里的真名，例如 rsaUuid 不是 uuid）
- 无 Cookie 能否取钥：
- 密钥是否静态（多次 GET 是否同一 uuid/公钥）：

## 4. 已排除（防止下轮重测）

- （主站 JS 无路径、SPA 壳 404、NXDOMAIN、错 Host、错字段空数组……一行一条）

## 5. 凭证与敏感串（脱敏）

| 类型 | 值（打码） | 出处 | 有效性 |
|---|---|---|---|
| | | | |

完整密钥/账号不进本表。原料另放 `hunts/<目标>/raw/`。

## 6. 待办

- [ ] （只写下一步动作，不写感想）

## 7. 覆盖度

```
✅已测：
❌未测：
🔄变种：
💡关联：
失败升级已到：L?
```
